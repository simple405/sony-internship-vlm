# Experiments

Use this directory for small, reviewable VLM experiment records:

- dataset slices and sample manifests
- prompt or model comparison notes
- eval specs, metric definitions, and frozen expected outputs
- short qualitative error-analysis notes

Keep large images, generated outputs, logs, and API responses under `vlm/data/` or `vlm/tmp/`.
