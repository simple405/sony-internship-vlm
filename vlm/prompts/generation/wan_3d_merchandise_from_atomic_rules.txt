IMAGE 1 HAS HIGHEST PRIORITY FOR ALL CHARACTER IDENTITY DETAILS.

Use the provided 2D anime character image as the primary visual identity reference.
Use atomic_rules as mandatory reinforcement constraints.

CORE TASK:
Create a derivative 3D anime merchandise design sheet for the specified product type.

PRODUCT TYPE:
{{PRODUCT_TYPE}}

PRODUCT SCOPE:
{{PRODUCT_SCOPE}}

PRODUCT-SPECIFIC REQUIREMENTS:
{{PRODUCT_BLOCK}}

IDENTITY PRESERVATION:
- Preserve the character identity from the 2D reference image as closely as possible.
- Atomic_rules are coarse identity constraints, not a replacement for visible reference-image details.
- Preserve all product-relevant visible character features.
- Preserve exact visible color distribution, gradients, split colors, patterns, symbols, trims, and decorative markings.
- Do not collapse multicolor or gradient objects into one flat solid color.
- If a detail is visible in the 2D image but not listed in atomic_rules, still preserve it when it is important to character identity.
- If atomic_rules include body features that are not visible or not applicable to the product type, do not force them into the product.
- If text conflicts with the image, follow the image.
- If atomic_rules conflict with unclear image details, use atomic_rules as mandatory constraints.
- Do not invent new major character features that are not present in the reference or atomic_rules.

MANDATORY ATOMIC_RULES:
{{ATOMIC_RULES}}

RULE VISIBILITY POLICY:
{{RULE_VISIBILITY_POLICY}}

OUTPUT FORMAT:
- Generate exactly one clean merchandise design sheet.
- Use a clean white studio background.
- Do not include the original 2D image inside the output.
- Do not include text, labels, captions, logos, UI elements, or watermarks.
- Keep all generated views consistent with each other.

VIEW CONSISTENCY:
- The generated views must describe the same physical merchandise item.
- Keep scale, proportions, material, colors, motifs, and decorations consistent across views.
- Do not generate unrelated product variations in different views.
- Do not generate extra views unless the selected product block explicitly asks for them.

COLOR AND PATTERN PRESERVATION:
- Preserve the exact visible color distribution from the 2D reference image, not only the nearest simple color name.
- For every visually important area, keep local color zones, split colors, gradients, ombre effects, transparent or translucent color changes, metallic tint shifts, highlights, trims, symbols, and surface patterns.
- Pay special attention to small identity-defining objects such as handheld items, head accessories, hair ornaments, bows, ribbons, back accessories, legwear, footwear, and decorative panels.
- If an object is multicolor, gradient, patterned, translucent, shiny, starry, or has colored decorative markings in the 2D reference, the 3D version must preserve those visual properties.
- Do not collapse a multicolor or gradient object into one flat solid color.
- Do not replace visible color placement with a generic dominant color.
- If an atomic_rules color value is broad, such as a color family or a combined label like blue/cyan, treat it as a minimum color-family constraint and use the 2D reference image for the detailed hue distribution, gradients, decorations, and color placement.
- Preserve left/right color asymmetry and repeated color motifs consistently across all requested views.

NEGATIVE PROMPT:
text, letters, logo, watermark, caption, label,
original reference image pasted into output,
wrong colors, missing identity features, simplified design,
flat single-color replacement of multicolor or gradient objects,
lost color zones, lost gradients, lost patterns, lost decorative markings,
extra unrelated accessories, extra characters, blurry, low detail,
inconsistent product type, inconsistent colors between views,
unrequested full-body character figure, unrequested product variations
